  image=`echo $f | cut -d '_' -f2 | cut -d '.' -f1`
done
image=`echo $image | sed 's/^0*//'`
image=$((image+1))

shopt -u nullglob

mkdir /dev/shm/mjpeg
raspimjpeg -ic $image -vc $video > /dev/null &

exit 0
